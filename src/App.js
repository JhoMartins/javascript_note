import { Fragment } from 'react';
import { Notification, Section } from 'rbx';
import './App.scss';
import Router from './router';

const App = () => {
  return(
    <Fragment>
      <Router/>
    </Fragment>
  )
}

export default App;
